
  # FinQuest (Community)

  This is a code bundle for FinQuest (Community). The original project is available at https://www.figma.com/design/g7h95yLAD4lzyFWpf7OzYT/FinQuest--Community-.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  